import { Button } from 'react-native-paper';



const Buttoncpm = ({text, onPress}) => {
    return(
        <Button mode="contained" style={{}} onPress={() => onPress()}>{text}</Button>
    )
}

export default Buttoncpm